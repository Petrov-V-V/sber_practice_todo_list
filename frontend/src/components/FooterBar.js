import React from 'react';
import { Layout } from 'antd';



const { Footer } = Layout;

const FooterBar = () => {
  return (
    <Footer style={{ textAlign: "center", zIndex: 0 }}>Made with ❤</Footer>
  );
}

export default FooterBar;